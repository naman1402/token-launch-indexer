type Token @entity {
  id: String!
  name: String
  symbol: String
  decimals: Int
}

type Pool @entity {
  id: String!
  token0: Token!
  token1: Token!
  lpType: String
  launchBlock: Int
  launchTimestamp: Int
}
